We need to update the Passenger App to align with the backend service naming system.

----------------------------------------------------
1 — CRITICAL CHANGE (Naming Fix)
----------------------------------------------------
Replace ALL occurrences of:

"bike"

with:

"scooter"

This includes:
- service identifiers
- UI labels
- service mapping logic
- navigation routes (if any)
- conditional rendering
- icons mapping

----------------------------------------------------
2 — BACKEND INTEGRATION (IMPORTANT)
----------------------------------------------------
The backend now sends ONLY:
serviceType = "scooter"

This comes from:
GET /services/control
and socket event:
service:control:changed

The app must use this value directly as the service key.

----------------------------------------------------
3 — SERVICE MAPPING RULE
----------------------------------------------------
Update service mapping so that:

scooter service is connected to backend data only
No hardcoded "bike" logic should exist anywhere.

----------------------------------------------------
4 — UI REQUIREMENTS
----------------------------------------------------
For scooter service:

- Label: "Scooter"
- Icon: reuse existing bike/motorcycle icon if needed
- Must be fully driven by backend state:
  isEnabled
  displayMode
  maintenanceEta
  unavailableMessage

----------------------------------------------------
5 — REAL-TIME SUPPORT (DO NOT BREAK)
----------------------------------------------------
Ensure socket listener still works:

service:control:changed

It must correctly update scooter service in real-time.

----------------------------------------------------
6 — CRITICAL RULE
----------------------------------------------------
- DO NOT create any fallback for "bike"
- DO NOT hardcode service values
- ONLY rely on backend "scooter"

----------------------------------------------------
EXPECTED RESULT
----------------------------------------------------
- Bike is completely removed from the app
- Scooter is fully functional and connected to backend
- Service updates in real-time without refresh