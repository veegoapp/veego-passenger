We are continuing the migration of the Passenger App to the new ActiveSession Recovery System.

Attached is the backend Mobile Integration Contract.

Treat that contract as the ONLY source of truth.
Do not guess any backend fields or invent data structures.

Current project status:

Completed:
- ActiveSession API client exists.
- ActiveSession adapter exists.
- ActiveSessionContext exists.
- Cold-start recovery now uses ActiveSession instead of GET /rides/active.
- Car / Scooter / Delivery recovery now uses ActiveSession.
- Ride startup no longer depends on the old recovery endpoint.
- Foreground recovery and socket reconnect recovery already use ActiveSession.
- app/index.tsx no longer uses GET /rides/active.

There is only one temporary migration bridge remaining for Shuttle.

Current architecture:

BookingContext still creates a synthetic Booking object from ActiveSession because:
- ticket.tsx
- app/(tabs)/trips.tsx

still depend on BookingContext recovery state.

This bridge was intentionally temporary.

Goal of this task:

Make ActiveSessionContext the single source of truth for Shuttle recovery.

Scope of this task ONLY:

Audit only these files:

- context/BookingContext.tsx
- app/ticket.tsx
- app/(tabs)/trips.tsx

Before changing anything:

Report:

1. Which recovery fields are currently read from BookingContext.

2. Which of those fields already exist in PassengerShuttleSession.

3. Which displayed fields still require REST enrichment because they are NOT present in the ActiveSession contract.

Then implement:

1. BookingContext

Remove only recovery ownership.

Remove recovery-related state that exists only because of the old recovery system, such as:
- activeBooking hydration from ActiveSession
- confirmedBookingId recovery state
- confirmedTripId recovery state
- confirmedBookingStatus recovery state

Keep BookingContext responsible only for booking-flow UI state, including:
- booking creation
- pending booking flow
- selected route
- booking sheet state
- other temporary booking UI state

Do not break the booking flow.

2. ticket.tsx

Stop reading Shuttle recovery data from BookingContext.

Instead, read it directly from ActiveSessionContext.

When the active session is a shuttle session, use the ActiveSession contract as the source of truth for:

- bookingId
- bookingStatus
- trip.id
- trip.status
- trip.route
- departureTime
- arrivalTime
- boardingStation
- driver information

Do not invent missing fields.

If ActiveSession does not contain a field, preserve the existing REST enrichment behavior if one already exists.

3. app/(tabs)/trips.tsx

The active Shuttle card should read directly from ActiveSession.

Do NOT change:

- historical trips list
- history APIs
- history screens

Only migrate the active-session portion.

4. Remove the temporary bridge

Remove the synthetic Booking creation introduced only for migration.

Remove fake values such as:

- synthetic route.path
- synthetic route.code
- synthetic seat placeholders

If a field is unavailable from ActiveSession, leave it to the existing REST enrichment instead of creating fake data.

Do NOT modify:

- backend
- ActiveSession contract
- ActiveSessionContext
- Ride recovery
- Car/Scooter/Delivery logic
- navigation architecture

After implementation provide:

- Files changed.
- Exactly what synthetic Booking logic was removed.
- Remaining responsibilities of BookingContext.
- Which Shuttle fields still require REST enrichment.
- TypeScript result.