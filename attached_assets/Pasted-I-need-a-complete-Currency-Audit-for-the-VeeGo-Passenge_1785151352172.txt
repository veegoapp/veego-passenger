I need a complete Currency Audit for the VeeGo Passenger App.

Important:
- This is an investigation only.
- DO NOT modify any files.
- DO NOT fix anything yet.
- I need a detailed report first so we can apply all fixes in one controlled pass.

Context:
VeeGo is an Egypt-only ride/shuttle application.
The only supported currency should be Egyptian Pound (EGP).
There should be NO USD, TND, SAR, AED, EUR, or any other currency displayed anywhere in the passenger app.

Your task:
Perform a complete audit of the passenger app codebase and find every place where currency can appear incorrectly.

Check ALL of the following:

1) Hardcoded currency symbols and labels
Search for:
- $
- USD
- DT
- TND
- SAR
- AED
- EUR
- €
- £
- any other foreign currency symbols or names

Report:
- File path
- Line number
- Current code
- What the user sees
- Expected EGP-only behavior
- Severity level

---

2) Money formatting functions
Audit all:
- formatCurrency functions
- price formatters
- toLocaleString usage
- Intl.NumberFormat usage
- currency formatting helpers

Check for:
- en-US locale being used incorrectly
- missing currency labels
- incorrect currency codes
- device locale dependency
- inconsistent Arabic/English output

---

3) Translation / i18n audit
Check:
- Are currency labels coming from translation keys?
- Are there hardcoded "EGP" strings bypassing translations?
- Does Arabic show "ج.م" or "جنيه" correctly?
- Does English show "EGP" correctly?
- Are any missing translation keys related to money?

Report every issue.

---

4) API response currency handling
Audit every API/service that returns money values:

Examples:
- ride estimate
- ride creation response
- fare
- trip details
- wallet
- payments
- refunds
- promotions
- discounts
- cancellation fees

Check:
- Does the app trust backend currency blindly?
- Does it validate EGP?
- Are there places where currency is assumed instead of enforced?

---

5) Payment method screens
Check all payment-related UI:

- Cash
- Wallet
- Any future payment methods

Look for:
- wrong currency labels
- wrong icons (example: dollar symbol for cash)
- wrong refund messages
- wrong money formatting

---

6) Ride flow screens
Audit all screens where passenger sees money:

Examples:
- Ride options
- Fare estimate
- Searching driver
- Driver assigned
- Active ride
- Completed ride
- Ride history
- Trip details
- Cancellation screens
- Wallet screens
- Promotions

Check every displayed amount.

---

7) Search the whole repository
Do not only check obvious files.

Scan:
- app/
- components/
- hooks/
- services/
- utils/
- lib/
- translations/
- constants/

---

Final report format:

A) Executive Summary
- Is the Passenger App fully EGP-only?
- Total number of issues found.
- Severity breakdown.

B) Complete Issue Table:

| # | File | Line | Problem | Current Behavior | Expected Behavior | Severity |

C) Currency Display Map:
List every screen that displays money and confirm:
✅ Correct EGP
or
❌ Wrong currency/problem

D) API Currency Audit:
List every API endpoint/service checked and whether currency handling is correct.

E) Translation Audit:
List missing keys or hardcoded currency strings.

F) Files that require changes:
Only list files.
Do not modify anything.

Remember:
The goal is to discover ALL currency-related problems before any fixes are applied.