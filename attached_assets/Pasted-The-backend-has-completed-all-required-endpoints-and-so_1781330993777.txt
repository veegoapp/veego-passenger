The backend has completed all required endpoints and socket events. Wire everything into the passenger app now. Do not change any UI or design — only add the missing logic.

---

**Task 1 — Ride Price Estimate before booking (app/(tabs)/car.tsx)**
- After the user selects both pickup and dropoff locations, automatically call:
  `GET /rides/estimate?pickupLat=&pickupLng=&dropoffLat=&dropoffLng=&serviceType=`
- Show the estimated price, distance, and duration above the "Confirm Ride" button
- Show a loading spinner while fetching
- If the estimate fails, show "Price unavailable" and still allow the user to confirm
- On surge: show a warning that prices are higher than usual

---

**Task 2 — Waiting Charge live meter (app/(tabs)/car.tsx)**
The backend now emits these socket events when the driver is waiting:
- `ride:waiting:charge:started` → payload: `{ rideId, startedAt, ratePerMinute }`
- `ride:waiting:charge:updated` → payload: `{ rideId, elapsedMinutes, currentCharge }`
- `ride:waiting:charge:capped` → payload: `{ rideId, finalCharge, cappedAt }`

Wire them in `src/hooks/useRide.ts`:
- On `started`: show a waiting charge banner with the rate per minute
- On `updated`: update the displayed charge amount live
- On `capped`: show "Maximum waiting charge reached" and stop the meter
- Hide the banner when the ride moves to `active` status

---

**Task 3 — Pre-departure driver location on Shuttle (app/ticket.tsx + app/trip-detail.tsx)**
The backend now emits `shuttle:driver:location` before departure too, not only during the trip.
Payload: `{ tripId, lat, lng, heading }`

- In both `ticket.tsx` and `trip-detail.tsx`, add a socket listener for `shuttle:driver:location`
- Update the driver marker position on the map whenever a new location arrives
- This should work in all trip phases: scheduled, boarding, and active

---

**Task 4 — Structured error codes (all screens)**
The backend now returns `{ message: "...", code: "MACHINE_CODE" }` on all errors.

Create a mapping file at `src/utils/errorMessages.ts`:
```typescript
export const ERROR_MESSAGES: Record<string, string> = {
  SEAT_COUNT_EXCEEDED: "Not enough seats available on this trip",
  TRIP_FULLY_BOOKED: "This trip is fully booked",
  PROMO_EXPIRED: "This promo code has expired",
  PROMO_LIMIT_REACHED: "This promo code is no longer available",
  PROMO_USER_LIMIT_REACHED: "You have already used this promo code",
  INSUFFICIENT_BALANCE: "Your wallet balance is insufficient",
  RIDE_ALREADY_ACTIVE: "You already have an active ride",
  PRICING_UNAVAILABLE: "Pricing is currently unavailable, please try again",
  INVALID_REQUEST: "Invalid request, please check your inputs",
  RIDE_NOT_FOUND: "Ride not found",
  BOOKING_DUPLICATE: "You already have a booking on this trip",
};

export function getErrorMessage(code?: string, fallback?: string): string {
  if (code && ERROR_MESSAGES[code]) return ERROR_MESSAGES[code];
  return fallback || "Something went wrong, please try again";
}
```

Then replace all raw `error.response?.data?.message` displays in these files with `getErrorMessage(error.response?.data?.code, error.response?.data?.message)`:
- `app/(tabs)/car.tsx`
- `app/(tabs)/wallet.tsx`
- `components/TripSheet.tsx`
- `app/support.tsx`

---

**Task 5 — Ride status real-time updates (src/hooks/useRide.ts)**
The backend now emits `ride:status:changed` on every state transition.
Payload: `{ rideId, status, previousStatus, timestamp, meta }`

- Add a socket listener for `ride:status:changed` in `useRide.ts`
- Update the ride phase/status in local state immediately when received
- Handle all statuses: `searching`, `driver_assigned`, `driver_arrived`, `active`, `completed`, `cancelled`
- On `driver_assigned`: update driver info from `meta.driverId` and `meta.driverName`
- On `completed`: trigger the rating sheet, store `meta.finalPrice`
- On `cancelled`: show the cancellation reason from `meta.cancelledBy` and `meta.message`
- This replaces any existing polling logic for ride status if present

---

**Task 6 — Full ride data on deep link (app/trip-tracking.tsx)**
When the app opens via `veego://ride/{id}`, it navigates to `/trip-tracking?id={id}`.
The screen must now load the full ride data using: `GET /rides/:id`

- On mount, if an `id` param exists, fetch `GET /rides/:id`
- Populate all available fields: driver info, vehicle, status, estimated price, timestamps
- Show a loading state while fetching
- If the ride is already completed or cancelled, redirect to `/receipt?id={id}` instead
- If `RIDE_NOT_FOUND` or `FORBIDDEN`, show an error screen with a "Go Home" button

---

After completing all tasks, provide a report listing every file changed and exactly what was added in each one.