You are working on the Passenger App repository only.

The attached document:
"ActiveSession Recovery System — Mobile Integration Contract"

is the ONLY source of truth for:
- GET /api/passenger/session
- session:snapshot behavior
- session types
- status handling

Previous phase is already completed:
- ActiveSession API client exists.
- ActiveSession types exist.
- ActiveSession adapter exists.
- ActiveSessionProvider exists.
- Socket session:snapshot support exists.

Do not rebuild those parts.
Do not modify backend code.

Goal of this task:
Integrate ActiveSession into the app cold-start recovery flow.

The objective:
Replace the old "active ride discovery" decision at app startup with ActiveSession as the primary recovery source.

The old system:
- app/index.tsx
- GET /rides/active
- resumeService route parameter

must no longer be the source of truth for deciding whether the user has an active session.

---

Required changes:

## 1. Update app startup recovery flow

Modify:

app/index.tsx

Current behavior:
- After authentication, app checks GET /rides/active.
- It decides whether a ride exists.
- It passes only resumeService.

New behavior:

After authentication is valid:

1. Initialize ActiveSession.
2. Wait for ActiveSession loading to complete.
3. Use the normalized ActiveSession state to decide recovery.

The startup decision must be based on:

ActiveSession:

- null:
  - continue normal app startup.

- ride:
  - vehicleType = car
  - vehicleType = scooter
  - vehicleType = delivery

  Restore the correct service flow.

- shuttle_booking:
  Restore shuttle recovery flow.

---

## 2. Create recovery navigation decision logic

Do not put session-type checks directly inside multiple screens.

Create a small helper if needed.

Suggested location:

src/session/activeSessionNavigation.ts

Responsibilities:

Convert normalized ActiveSession into a recovery destination.

It should define:

Ride:
- car → car recovery destination
- scooter → scooter recovery destination
- delivery → delivery recovery destination

Shuttle:
- shuttle_booking → shuttle recovery destination

No active session:
- normal home flow

Important:

Do not invent routes.
First inspect existing Expo Router routes and reuse existing destinations.

---

## 3. Preserve existing UI behavior temporarily

This is a migration phase.

Do NOT:

- remove GET /rides/active yet
- delete resumeService handling yet
- delete old recovery code
- modify useRide
- modify CarServiceScreen
- modify BookingContext
- modify ticket.tsx
- modify trip-detail.tsx

The goal is to introduce ActiveSession as the new decision source without breaking current flows.

---

## 4. Restore identifiers through navigation state

The recovery decision must not pass only:

resumeService

The new recovery state should preserve the identifiers required by later phases:

Ride:
- rideId
- vehicleType
- status

Shuttle:
- bookingId
- trip data identifier if available
- booking status

Do not duplicate the whole session payload into route params.

The ActiveSessionProvider remains the source of truth.

---

## 5. Handle API failure correctly

Important:

If:

GET /api/passenger/session

fails:

Do NOT assume:

"no active session"

Do NOT clear the session.

Behavior:

- preserve existing ActiveSession state.
- allow normal startup fallback.
- log the failure using existing project logging style.

---

## 6. Validate authentication order

Confirm that:

ActiveSession initialization happens only after:
- valid authentication
- access token availability

Do not create unauthenticated session calls.

---

## Constraints

Do not modify:

- backend
- ActiveSession contract
- ActiveSession types unless a real mismatch is discovered
- ActiveSession adapter unless a real mismatch is discovered
- socket implementation

Do not implement yet:

- foreground recovery
- reconnect recovery
- Shuttle screen hydration
- Car/Scooter/Delivery screen migration

Those are separate phases.

---

Validation required:

Before finishing run TypeScript checks.

Return:

1. Files modified.
2. Startup recovery flow before vs after.
3. Confirmation that:
   - ActiveSession is now the primary cold-start decision source.
   - Old recovery code still exists for safety.
   - No service screens were migrated yet.
4. TypeScript result.
5. Any blockers.