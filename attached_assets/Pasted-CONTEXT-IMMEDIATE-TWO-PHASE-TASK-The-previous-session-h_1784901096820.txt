CONTEXT & IMMEDIATE TWO-PHASE TASK:
The previous session hit token limits mid-way while refactoring components/car/CarServiceScreen.tsx and related UI files.

You need to inspect the current state of the code and execute the following two phases sequentially.

PHASE 1: Complete Search Box & Bottom Sheet UI Refactoring
Fix and complete the search inputs container ("Your Location" & "Where to?") and bottom sheet layout across Car, Scooter, and Delivery screens without touching any API or backend logic.

Floating Search Box & Glassmorphism:

Style the Search Box Container containing both "Your Location" and "Where to?" fields as a sleek floating glassmorphic card.

Apply proper opacity and blur (rgba(255, 255, 255, 0.92) + BlurView or fallback).

Fix text contrast: #1A1A1A for labels/titles ('Your Location', 'Current Location') and #666666 for placeholders ('Where to?' / 'Where are you going?') so all text inside the search box is crisp and readable.

Interactive Location Fields:

Make the "Your Location" row inside the search box fully clickable so users can tap it to open the pickup location picker modal.

Sheet Expansion Behavior (NO Full-Screen Navigation):

Clicking "Where to?" inside the search box MUST NOT navigate to a separate full-screen page (Choose your destination).

Instead, clicking "Where to?" must smoothly expand/slide the search container and sheet upwards over the map, revealing the full search input and Recents list directly INSIDE the expanded sheet while keeping the map visible behind it.

Do NOT render any 'A' or 'B' letter badges inside the search fields.

PHASE 2: Fix Service State Leakage (Isolate Car, Scooter, & Delivery)
After completing Phase 1, address the core functional state bug:

Scoped Active Ride State:

Bind active ride phases (searching, driver_assigned, in_ride) strictly to their specific serviceType (car, scooter, delivery).

Isolated Tab Switching:

If a user starts searching for a driver in Car, switching tabs to Scooter or Delivery must render those services in their normal/idle phase (NOT in the active searching state).

Each service tab must maintain its own clean, isolated state.

EXECUTION INSTRUCTION:
Please start by inspecting the existing code from the last partial edit. Complete Phase 1 first, then proceed immediately to Phase 2."