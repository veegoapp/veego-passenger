You are working on the VeeGo Rider App (Expo SDK 54 / React Native / Expo Router). Complete all 4 tasks in order. After finishing, output a report titled "## What was done".

---

TASK 1 — Enforce RTL layout for Arabic

Location: app/_layout.tsx, constants/i18n.ts, and all screen files

What to build:
- When the user selects Arabic as their language, the entire app layout must switch to RTL
- In app/_layout.tsx (or ThemeProvider.tsx): after reading the language from AsyncStorage, call I18nManager.forceRTL(true) if language is 'ar', and I18nManager.forceRTL(false) if 'en' — then reload the app using expo-updates or a state reset so the RTL change takes effect
- Audit all StyleSheet definitions across every screen and component for hardcoded left/right values (marginLeft, paddingLeft, marginRight, paddingRight, textAlign: 'left'/'right', flexDirection: 'row') — replace them with RTL-safe equivalents:
  - marginLeft → marginStart
  - marginRight → marginEnd
  - paddingLeft → paddingStart
  - paddingRight → paddingEnd
  - textAlign: 'left' → textAlign: 'auto' (React Native respects RTL automatically with 'auto')
  - flexDirection: 'row' that contains text → keep as is (React Native mirrors row automatically in RTL)
- Pay special attention to: auth.tsx, car.tsx, routes.tsx, wallet.tsx, profile.tsx, trip-detail.tsx, ticket.tsx
- After making changes, verify that all icon+text rows, input fields, and buttons are correctly mirrored in RTL

---

TASK 2 — Complete Arabic translation audit

Location: constants/i18n.ts

What to do:
- Go through the EN translations object key by key
- For every key that exists in EN but is missing from AR (or has an identical English value in the AR object suggesting it was not translated), add the correct Arabic translation
- Also add all new keys that were added in Phase 1, 2, and 3 of this project — make sure every single key has both an EN and AR value
- Do not remove any existing keys
- Output a list of every key you added or fixed in the ## What was done section

---

TASK 3 — Persist biometric and 2FA toggles

Location: app/(tabs)/profile.tsx and src/hooks/useProfile.ts

What to fix:
- Biometric (Face ID / fingerprint) toggle and 2FA toggle in the profile screen are local React state only — they reset on every app reload
- The backend has PATCH /users/me which accepts arbitrary profile fields
- Do the following:
  1. On profile screen mount, read biometricEnabled and twoFactorEnabled from the user object returned by GET /users/me (if the backend returns these fields) — fall back to AsyncStorage keys veego_biometric and veego_2fa if not in the API response
  2. When either toggle changes: save to AsyncStorage immediately (for instant persistence), AND call PATCH /users/me with { biometricEnabled, twoFactorEnabled } — if the PATCH fails, revert the toggle and show an error toast
  3. Biometric toggle should also call expo-local-authentication to verify the user's biometric before enabling it
- Add i18n keys if needed (EN + AR)

---

TASK 4 — Remove static fallback and fix stations error state

Location: app/stations.tsx and constants/data.ts

What to fix:
- app/stations.tsx silently falls back to stale static data from constants/data.ts when the API call to GET /routes/:routeId/stations fails
- This masks real errors and shows outdated station info
- Do the following:
  1. Remove the fallback logic that reads from constants/data.ts in stations.tsx
  2. Replace it with an explicit error state: full-screen error card with an icon, the message "Could not load stations. Please check your connection and try again.", and a Retry button that re-triggers the query
  3. Keep constants/data.ts in the codebase but add a comment at the top: /* Static fallback data — no longer used in production. Safe to delete after confirming all routes load from API. */
  4. Also remove the static fallback for shuttle lines in app/(tabs)/routes.tsx if one exists — apply the same error state + retry pattern
- Add i18n keys for error messages (EN + AR)

---

After completing all 4 tasks output:
## What was done
- Task 1: [every file modified, what StyleSheet properties were changed]
- Task 2: [list every i18n key added or fixed]
- Task 3: [files modified, what changed]
- Task 4: [files modified, what changed]