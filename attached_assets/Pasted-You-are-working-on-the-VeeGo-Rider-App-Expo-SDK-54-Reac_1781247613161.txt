You are working on the VeeGo Rider App (Expo SDK 54 / React Native / Expo Router). Backend base URL is EXPO_PUBLIC_API_URL. Auth token sent as Bearer. Complete all 4 tasks in order. After finishing, output a report titled "## What was done".

---

TASK 1 — Replace MOCK_CARDS with real wallet top-up and payment flow

Location: app/(tabs)/wallet.tsx and app/(tabs)/profile.tsx

What to fix:
- MOCK_CARDS = [] means the card management UI always shows empty
- The backend in the current phase supports wallet top-up via POST /wallet/topup (amount only — no card integration yet)
- Do the following:
  1. Remove the MOCK_CARDS constant and any card-list UI that depends on it from both wallet.tsx and profile.tsx
  2. In wallet.tsx: keep the top-up flow using POST /wallet/topup — make sure the quick top-up buttons (50/100/200/500 EGP) work correctly and show a success confirmation after top-up
  3. Add a "Payment methods" section placeholder with a single item: "Cash" marked as active, and a note: "Card payments coming soon" — this is UI only, no API call
  4. In profile.tsx: remove the card list section entirely, replace with a simple "Payment: Cash only (cards coming soon)" info row
- Add i18n keys for all new strings (EN + AR)

---

TASK 2 — Featured promos listing

Location: src/hooks/usePromos.ts and app/promo.tsx

What to fix:
- usePromos always returns promos: [] because no GET endpoint is called
- The backend has GET /promo/validate for validation but no public listing endpoint
- Do the following:
  1. In usePromos.ts: add a query that calls GET /promo — if the endpoint returns 403 or 404 (admin-only), catch the error silently and return promos: [] — do not crash
  2. In app/promo.tsx: if promos list is empty, show a friendly empty state: icon + "No featured promos right now. Enter a code manually." instead of a blank section
  3. The manual promo code entry and POST /promo/validate flow must still work as before
- Add i18n keys (EN + AR)

---

TASK 3 — Geocoding / place suggestions for pickup and dropoff

Location: app/(tabs)/index.tsx and app/(tabs)/car.tsx (wherever MOCK_LOCATIONS = [] is used)

What to build:
- Replace MOCK_LOCATIONS = [] with real place suggestions using the backend's saved locations API:
  1. On app startup (after login), call GET /user/locations to fetch the user's saved locations (home, work, other)
  2. Show these saved locations as the first suggestions in the pickup/dropoff search field, even before the user types anything
  3. When the user types 3+ characters in the search field, call the backend GET /user/locations (filtered client-side by the typed text matching name or address fields)
  4. If no saved locations match, show an empty state: "No saved locations found. You can save locations from your profile." 
  5. Add a "Save this location" option after a completed ride that calls POST /user/locations with { label: 'other', name, address, latitude, longitude, isDefault: false }
- Add i18n keys (EN + AR)

NOTE: Do not integrate any third-party geocoding API (Google Places, Mapbox etc.) — use only the backend saved locations endpoints for now.

---

TASK 4 — Replace scooter and bike mock data with real service controls

Location: components/bike/BikeServiceScreen.tsx, components/bike/bikeMapData.ts, components/scooter/scooterMapData.ts

What to fix:
- bikeMapData.ts and scooterMapData.ts are 100% static mock data
- The backend controls service availability via GET /services/control and GET /services/:type/control
- ServiceControlContext already fetches this — use it
- Do the following:
  1. For both bike and scooter service screens: remove the static mock data files (bikeMapData.ts, scooterMapData.ts) or mark them clearly as /* REMOVED — replaced by real service control */
  2. Both bike and scooter service screens should show the service status from ServiceControlContext:
     - If isEnabled === false and displayMode === 'coming_soon' → show a "Coming soon" screen with the unavailableMessage from the backend
     - If isEnabled === false and displayMode === 'unavailable' → show an "Unavailable" screen with the message
     - If isEnabled === true → show the normal service UI (map + request flow, same as car.tsx pattern)
  3. Wire up the scooter and bike ride request to the same useRide hook as car, passing vehicleType: 'motorcycle' for scooter/bike
  4. The map, driver searching, and live tracking screens are already built for car — reuse them, just pass the correct vehicleType
- Add i18n keys if needed (EN + AR)

---

After completing all 4 tasks output:
## What was done
- Task 1: [files modified, what changed]
- Task 2: [files modified, what changed]
- Task 3: [files modified, what changed]
- Task 4: [files created/modified/deleted, what changed]