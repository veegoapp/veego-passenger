We are continuing the Map Architecture refactor.

Phase 1 is already completed successfully:
- useAnimatedDriverMarker.ts created
- useMapCamera.ts created
- useGoogleRoute.ts created
- DriverMarker.tsx created

Before changing anything, read the current implementations of:
- components/car/CarMap.tsx
- components/shared/PassengerTrackingMap.native.tsx
- the new extracted hooks

Your task is Phase 2 only: migrate existing components to use the extracted logic safely.

IMPORTANT RULES:
- Do NOT rewrite the map components.
- Do NOT change screen behavior.
- Do NOT change props contracts.
- Do NOT remove old logic until the replacement is verified.
- Make changes one component at a time.
- After each component migration, run TypeScript/build checks and report results.

==================================================
STEP 1 — Migrate PassengerTrackingMap.native.tsx ONLY
==================================================

Start with components/shared/PassengerTrackingMap.native.tsx.

Replace duplicated logic with the new shared modules:

1) Replace AnimatedRegion logic:
Use:
useAnimatedDriverMarker()

Keep:
- same animation duration
- same stop-before-start behavior
- same marker positioning behavior

2) Replace map camera refs and helpers:
Use:
useMapCamera()

Replace:
- mapRef
- mapReadyRef
- pendingCameraRef
- runOrQueueCamera
- onMapReady

Keep all existing camera behavior:
- fit behavior
- follow behavior
- recenter behavior
- pan handling

3) Replace route fetching logic:
Use:
useGoogleRoute()

IMPORTANT:
PassengerTrackingMap has two route modes:
A) Shuttle mode:
- stations.length > 0
- route through stations/waypoints

B) Car/Scooter/Delivery tracking mode:
- stations.length === 0
- driver → pickup while arriving
- driver → dropoff after trip started

Preserve this separation.

Do not merge both flows into one confusing condition.

4) Replace driver marker JSX:
Use:
<DriverMarker />

Preserve:
- vehicle type behavior
- heading rotation
- MarkerAnimated behavior

Do not change station markers or shuttle-specific UI.

After completing PassengerTrackingMap:
- run type check
- report:
  - files changed
  - any behavior differences
  - any errors

STOP HERE.
Do not migrate CarMap yet.

==================================================
STEP 2 — Wait for approval
==================================================

After PassengerTrackingMap is verified, continue with CarMap migration separately.

==================================================
STEP 3 — Migrate CarMap.tsx
==================================================

Only after approval.

For CarMap preserve these two different phases:

A) PRE-BOOKING:
Passenger location → destination

Requirements:
- keep fetching route before driver assignment
- keep origin/destination pair deduplication
- keep nearby drivers behavior
- keep SearchingPulse behavior

B) ACTIVE RIDE:
Driver location → pickup/dropoff

Requirements:
- use useGoogleRoute()
- preserve 75 seconds / 300 meters throttle
- preserve route refresh behavior

IMPORTANT:
CarMap currently has a straight-line fallback when Directions fails:

[driver/user location] → destination

Do not remove this.
If useGoogleRoute does not provide coords, keep the fallback behavior.

Replace duplicated:
- AnimatedRegion
- camera refs
- driver marker rendering

with:
- useAnimatedDriverMarker
- useMapCamera
- DriverMarker

Do not change:
- user GPS acquisition
- NearbyDriversLayer
- SearchingPulse
- ride phase handling

==================================================
FINAL VERIFICATION
==================================================

After both components migrate, verify:

1. Car booking:
- user location works
- destination route works
- nearby drivers still appear

2. Driver accepted:
- driver marker animates smoothly
- camera follows correctly
- route switches to driver → pickup

3. Trip started:
- route switches to driver → dropoff

4. Shuttle:
- stations still render
- passenger stop logic still works
- completed leg logic still works

5. No duplicated:
- AnimatedRegion logic
- camera helper logic
- driver marker JSX
- route throttle constants

Proceed carefully and stop after each major step.