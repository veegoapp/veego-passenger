I need a deep root-cause investigation for 3 Passenger App ride-flow issues.

IMPORTANT:
Do not apply fixes immediately.
First analyze the complete flow and identify the real source of each problem.

Previous attempt:
The Google Directions routing issue was supposedly fixed before, but the problem still exists. Do not assume the previous fix is working. Verify the actual data flow from ride creation to map rendering.

========================================================
ISSUE 1 — Route is still a straight line instead of real roads
========================================================

Problem:
When a passenger selects pickup and destination and confirms a ride, the map draws a straight line between the two coordinates.

Expected:
The map should display the actual road route using Google Directions API data.

Context:
- Google Directions API is already configured in the backend.
- Previous attempt claimed this was fixed, but the passenger app still shows straight lines.

Investigate:

Backend:
- Find where Google Directions API is called.
- Verify whether a real route/polyline is generated.
- Verify the API response contains:
  - encoded polyline
  - route coordinates
  - distance
  - duration
- Check the ride creation response payload.
- Confirm whether the route data is actually sent to the Passenger App.

Passenger App:
- Find where the ride route polyline is created.
- Verify whether the app consumes backend Directions data.
- Check if the map component is receiving:
  - encoded polyline
  - decoded coordinates
- Look for fallback logic that creates:
  pickup -> destination
  straight line

Do not just check that Directions API exists.
Trace the complete path:
Google Directions → Backend → API response → Passenger App state → Map rendering.

Report the exact root cause.

========================================================
ISSUE 2 — "Reconnecting" appears during driver search
========================================================

Problem:
After passenger requests a ride and enters "Searching for driver", a "Reconnecting" message appears on the map screen.

However:
- The ride request reaches the driver successfully.
- Dispatch works.

Expected:
The user should not see a misleading reconnecting state during an active ride search unless the socket connection is actually unavailable.

Investigate:

- Find the source of the "Reconnecting" text/component.
- Identify which socket/network state triggers it.
- Check whether:
  - socket connection is recreated during ride request
  - reconnect state is stale
  - connection banner is shown incorrectly
  - ride search UI mounts before socket state stabilizes
  - passenger socket room subscription timing is wrong

Trace:
Passenger request → socket connection → ride searching screen → connection status UI.

Determine whether this is:
- a real socket problem
- a UI state synchronization problem
- a false positive status display

Report the exact cause.

========================================================
ISSUE 3 — Cash ride cancellation shows incorrect refund message and wrong amount
========================================================

Problem:
Passenger selected Cash payment.

After pressing Cancel:
The app shows a message similar to:
"Ride cancelled"
"50 EGP has been refunded to your wallet"

This is incorrect because:
- The ride was Cash payment.
- No wallet payment should have been refunded.
- The refund amount does not match the original ride price.

Investigate the complete cancellation flow:

Backend:
- Find passenger ride cancellation service.
- Check refund calculation logic.
- Check payment method conditions.
- Verify whether refunds should only happen for:
  - wallet payments
  - card payments
  - prepaid payments
- Check if cancellation response contains incorrect refund data.

Frontend:
- Find where the cancellation success message is displayed.
- Verify whether it always shows refund text regardless of payment method.
- Verify whether the displayed amount comes from backend response or hardcoded UI.

Trace:
Cancel button → API request → backend cancellation logic → response → Passenger App message.

Report:
- Why Cash rides show refund messages.
- Why the refund amount is incorrect.

========================================================
Final output required
========================================================

Before changing code, provide:

1. Root cause of each issue.
2. Exact files involved.
3. Whether the problem is frontend, backend, or both.
4. Recommended minimal fix.

Do not make unrelated changes.
Do not redesign UI.
Do not modify unrelated ride flow logic.