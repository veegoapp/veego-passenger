You are working on a Rider mobile app (Frontend) that consumes the Shuttle Service API.

I have attached the FINAL API CONTRACT file:
shuttle-rider-api-contract.md

────────────────────────────────────
🚨 TASK
────────────────────────────────────

Read the attached API contract carefully and use it as the SINGLE source of truth.

Your job is to:

1. Build / update the Rider app integration layer
2. Align ALL screens with the new Shuttle API
3. Ensure correct handling of:
   - trip status
   - booking flow
   - seat counters
   - error states
   - real-time updates

────────────────────────────────────
📦 REQUIRED INTEGRATION RULES
────────────────────────────────────

Use ONLY these status values:

- open
- active
- cancelled

DO NOT assume any other statuses.

────────────────────────────────────
🧠 CORE LOGIC TO IMPLEMENT
────────────────────────────────────

1. Trip List Screen
- Fetch: GET /shuttle/lines
- Display openTrips and activeTrips
- Show:
  - bookedSeats / totalSeats (14)
  - availableSeats
  - activation progress (7 seats threshold)
- Show message field directly from API

UI mapping:
- open → Yellow badge
- active → Green badge
- cancelled → Red badge

────────────────────────────────────
2. Trip Details Screen
- Fetch: GET /shuttle/lines/:id
- Render stations + trips list
- Each trip must show:
  - status
  - seats progress
  - "Needs X more bookings" message

────────────────────────────────────
3. Booking Flow
- POST /bookings

Rules:
- seatCount MUST always be 1
- wallet payment is required before success

Handle responses:

✔ 201 success → show booking confirmation
✔ 409 → show "Seats were just taken, try another trip"
✔ 400 → show validation errors

After booking:
- refresh trip data immediately

────────────────────────────────────
4. Cancel Booking Flow
- PATCH /bookings/:id/cancel

Rules:
- show success state
- refresh trip UI after cancellation
- do NOT assume trip becomes OPEN again if it was ACTIVE

────────────────────────────────────
5. Passenger List Screen
- GET /shuttle/trips/:id/passengers
- Display boarding list
- Show user name + status + seat info

────────────────────────────────────
6. REAL-TIME UPDATES (IMPORTANT)
────────────────────────────────────

If socket.io is available, subscribe to:

room: passenger:<userId>

Events:
- notification:new → trip cancelled or refunded
- booking:boarded → passenger boarded

Update UI instantly when events arrive.

────────────────────────────────────
7. EDGE CASE HANDLING
────────────────────────────────────

Must handle:

- 409 race condition (seat taken)
- insufficient wallet balance
- trip cancelled after booking
- full trip (14/14 seats)
- trip becomes active after booking

────────────────────────────────────
🎯 GOAL
────────────────────────────────────

Build a fully synchronized Rider app that exactly matches backend behavior with zero mismatch in:

- status logic
- seat counting
- booking lifecycle
- cancellation/refund behavior

Use the attached API contract as absolute source of truth.