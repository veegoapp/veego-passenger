we are continue our edit phases on shuttle interface
first  add push notification delivery when a pending trip flips to Active — so users get alerted even if the app is in the background when minimum capacity is reached.
then go in phase 6
Phase 6 : BookingConfirmedScreen Refactoring]
​Component Target: BookingConfirmedScreen (or TicketScreen / BoardingPassScreen as shown in screenshot 134611.jpg)
​Tasks to execute:
​Remove QR Code & Clean Layout:
​Completely remove the QRCode component (or image asset) from the center of the ticket card.
​Remove the small description text below it ("Show to driver").
​Adjust the remaining content inside the ticket card (Date, Passengers, Total) to expand or center elegantly vertically to avoid leaving an empty blank space at the bottom after removing the QR code.
​Fix Floating Text/Bugs:
​Locate and remove the random floating number "5" rendered directly beneath the "Booking confirmed!" heading. Ensure it doesn't leak into the view layout.
​Status Banner & Trip Status Refactoring:
​Warning Banner: Redesign the orange informational banner ("Booking confirmed — trip activates when minimum passengers are reached"). Give it a modern minimalist styling with rounded corners, clean padding, and a slightly higher contrast text color to fit the premium theme.
​Active Status Badge: Move or emphasize the green "● Active" indicator badge (currently sitting under where the QR code was) up into the top dark section of the ticket next to the Line number (e.g., Line L04), or display it clearly right under the route name so the driver and rider see it instantly.
​Time & UI Enhancements:
​Fix the timezone text suffix if necessary (e.g., if the user should see local time instead of the static UTC next to 13:00, adjust the date formatter).
​Ensure that the bottom buttons "View all trips" (Primary CTA) and "Back to home" (Secondary Link) are perfectly aligned and functional. "View all trips" must reset the stack and switch the tab to the MyTripsScreen.
