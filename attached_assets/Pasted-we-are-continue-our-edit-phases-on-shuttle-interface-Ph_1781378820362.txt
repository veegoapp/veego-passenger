we are continue our edit phases on shuttle interface
Phase 8 : Comprehensive Arabic Localization & RTL Layout Fix]
​Context Notice for AI: The backend database is completely localized, providing fully translated fields for all routes, lines, and stations. The current frontend is missing localized data-binding hooks and is causing severe layout wrapping bugs in Arabic mode (RTL). You must fix these root causes across the entire application once and for all.
​Tasks to execute:
​Fix Layout Text Wrapping in Service Cards (Top Grid):
​Review the Service Category Cards row shown in 134622_2.jpg.
​The Bug: In Arabic mode, the text is forcefully wrapping into vertical pieces (e.g., "توصيـ ل", "سكوتـ ر", "سيار ة") due to rigid layout boundaries.
​The Fix: Remove any fixed width constraints on the text containers. Implement proper flex styling, allow dynamic text scaling (numberOfLines={1} with adjustsFontSizeToFit), and apply safe horizontal padding so the Arabic service names fit comfortably in a single row without breaking or clipping.
​Strict Backend Data Translation Binding (Routes & Lines):
​The Bug: The application successfully localizes static station names but leaves the main Route/Line names in English (e.g., rendering "Ain Shams → El Maadi #1" instead of the Arabic equivalent from the API).
​The Fix: Locate the routing data mapping functions (inside HomeScreen, RoutesScreen, and RouteDetailsScreen).
​Modify the dynamic rendering tags to actively check the application's current language context (e.g., using i18n.language or your active locale selector):
​If locale is Arabic (ar), map the text directly to the Arabic fields returned by the API (e.g., render item.name_ar / item.title_ar or look inside the localized object structure).
​Do NOT fallback to the default English string (item.name) when the app is in Arabic mode if an Arabic string exists in the response.
​Global Dynamic Layout Guard (RTL Flex Direction):
​Audit all card components (RouteCard, PromoCard, TripCard). Ensure they use directional-agnostic layout alignments.
​Avoid hardcoded absolute paddings or directional flex rules that don't adapt. Use flexDirection: 'row' cleanly so React Native naturally mirrors the layout when switching between LTR and RTL via I18nManager.