You have already completed the P0–P2 fixes documented in FIXED_PASSENGER_APP_FINAL_REPORT.md. Now implement the following remaining tasks that require no backend changes — everything needed is already available in the codebase or the existing reports.

---

**Task 1 — SOS Button (SafetySheet.tsx)**
The SOS button currently does nothing. Fix it:
- When the user taps SOS, immediately call `Linking.openURL('tel:123')` (emergency number)
- Also fire `POST /rides/:id/sos` with the current ride ID and GPS coordinates (already available in the ride context)
- Show a confirmation message to the user after the call is triggered
- Do NOT remove or redesign the existing UI — only wire the action

---

**Task 2 — Delete Account (app/(tabs)/profile.tsx)**
The "Delete Account" button shows a confirmation dialog but does nothing after confirm. Fix it:
- On confirm, call `DELETE /users/me`
- On success: clear all tokens from SecureStore/AsyncStorage, reset all contexts, navigate to `/auth`
- On failure: show a user-friendly error message (not a raw server string)
- The backend endpoint exists — just wire the frontend

---

**Task 3 — Profile Photo Upload (app/(tabs)/profile.tsx)**
The image picker already opens and selects a photo but never uploads it. Fix it:
- After the user picks an image, send it via `POST /users/me/avatar` as multipart/form-data
- Show a loading indicator while uploading
- On success: update the displayed avatar in the profile screen
- On failure: show a user-friendly error and revert to the old photo
- The backend endpoint exists — just wire the frontend

---

**Task 4 — Wallet Transfer Button (app/(tabs)/wallet.tsx)**
The Transfer button is visible but leads nowhere and the backend endpoint does not exist yet. Fix it:
- Disable the button visually (greyed out)
- Show a small label under it: "Coming Soon"
- Do NOT remove it from the UI

---

**Task 5 — Bike/Scooter Service (components/bike/BikeServiceScreen.tsx)**
The Bike tab currently shows fake hardcoded data (MOCK_RIDER). Fix it:
- Remove all mock data
- Replace the screen content with a simple "Coming Soon" message, styled consistently with the rest of the app
- Keep the tab visible in the navigation but make it clear the service is not available yet

---

**Task 6 — Notification Deep Links for Rides (app/_layout.tsx)**
Currently `veego://ride/{id}` and `veego://promo/{code}` have no handlers. Fix it:
- Add a handler for `veego://ride/{id}` → navigate to `/trip-tracking?id={id}`
- Add a handler for `veego://promo/{code}` → navigate to `/promo?code={code}` (auto-fill the code)
- Follow the exact same pattern already used for `veego://shuttle/trip/{id}` in the same file

---

Important notes:
- Do not touch any backend files
- Do not change any UI design or layout — only wire the missing logic
- Do not modify any fixes already applied in FIXED_PASSENGER_APP_FINAL_REPORT.md
- After completing all tasks, give me a report listing exactly what was changed in each file