You are working on the Passenger App repository only.

The attached document:
"ActiveSession Recovery System — Mobile Integration Contract"

is the ONLY source of truth for:
- PassengerRideSession
- ride statuses
- car/scooter/delivery session fields
- session:snapshot behavior

Previous phases completed:

Phase 1:
- ActiveSession API layer exists.
- ActiveSession types exist.
- ActiveSession adapter exists.
- ActiveSessionProvider exists.

Phase 2:
- Cold start uses ActiveSession as the primary recovery decision source.
- Old recovery code still exists for migration safety.

Do not rebuild these parts.
Do not modify backend code.

Goal of this task:
Migrate Car, Scooter, and Delivery ride state consumption to ActiveSession.

The goal is:
ActiveSession becomes the authoritative source for the current ride snapshot.

Do NOT migrate Shuttle in this phase.

---

## 1. Update useRide integration

File:
src/hooks/car/useRide.ts

Current problem:
useRide owns too much recovery state locally.

It currently depends on:
- GET /rides/:id
- local React state
- socket events
- polling

Change direction:

ActiveSession should become the source for:
- rideId
- status
- driver information
- driver location
- fare
- waiting charge
- pickup/dropoff
- delivery recipient
- payment information

The hook should consume ActiveSession when:
sessionType === "ride"

---

Important:

Do NOT completely delete existing ride APIs.

Keep existing ride actions that are still required:
- request ride
- cancel ride
- accept actions
- UI commands

Only migrate recovery/state synchronization.

---

## 2. Create a ride session selector layer

Do not make every component read ActiveSessionContext directly.

Create a small selector/helper if needed.

Suggested:

src/session/activeRideSelectors.ts

Responsibilities:

Given ActiveSession:

Return:

- active ride session
- ride type:
  - car
  - scooter
  - delivery
- ride status
- driver data
- location
- pricing data
- delivery recipient

If session is:
- null
- shuttle_booking

return no active ride.

---

## 3. Replace duplicated ride recovery mapping

Remove duplicate interpretation of:

- vehicleType
- status
- driver data
- ride identifiers

from useRide where possible.

The ActiveSession adapter remains responsible for normalization.

Do not create another normalization system.

---

## 4. Socket behavior

Existing ride socket events may remain.

However:

When:
session:snapshot

is received:

ActiveSession should update first.

The ride UI should react to the updated ActiveSession state.

Do not create duplicate state conflicts where:
- socket event updates local ride state
- ActiveSession updates another state

Prefer ActiveSession as the final state source.

---

## 5. Polling behavior

Do not remove polling yet.

Before removing anything:

Inspect whether polling is still needed for fields not included in ActiveSession.

If polling remains:

It must not overwrite newer ActiveSession data incorrectly.

---

## 6. CarServiceScreen integration

File:
components/car/CarServiceScreen.tsx

Change only what is necessary.

The screen should be able to render from ActiveSession-backed ride state.

It should no longer require:
- manual user reopening
- old recovery state only

Do not redesign UI.

Do not change layouts.

---

## 7. trip-tracking integration

File:
app/trip-tracking.tsx

Migrate current ride restoration to use ActiveSession when available.

The screen should receive:

- rideId
- driver
- status
- location

from the centralized session state.

Do not migrate navigation rules in this phase.

---

## 8. Delivery requirements

Ensure Delivery keeps:

- recipient name
- recipient phone

from ActiveSession.

Do not lose these fields during migration.

---

## Constraints

DO NOT:

- modify backend
- modify ActiveSession contract
- migrate Shuttle
- modify BookingContext
- modify ticket.tsx
- modify trip-detail.tsx
- remove GET /rides/active yet
- remove GET /rides/:id yet
- rewrite all ride screens

This is a controlled migration.

---

## Validation

Before finishing:

Run TypeScript checks.

Return:

1. Files modified.
2. What ride state moved to ActiveSession.
3. What old ride recovery code remains.
4. Confirmation:
   - Car works
   - Scooter works
   - Delivery works
   - Shuttle untouched
   - No backend changes
5. TypeScript result.
6. Any blockers or assumptions.