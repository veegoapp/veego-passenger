You are working ONLY on the VeeGo Passenger App.

Objective:
Finalize and harden the Passenger App API integration based on the confirmed backend contract.

STRICT RULES:
- Do NOT modify the backend.
- Do NOT change API endpoints.
- Do NOT change business logic.
- Do NOT change UI, screens, navigation, or user flows.
- Do NOT refactor unrelated code.
- Do NOT rename files or move architecture unless absolutely required for this task.
- Only touch API integration, response mapping, and validation logic.
- Preserve all existing behavior.

Backend contract confirmation:
The backend passenger-facing APIs return camelCase consistently.

Official fields:
- isRead
- createdAt
- readAt
- userId
- tripId
- stationId
- latitude
- longitude

The backend does NOT require snake_case compatibility.

TASK 1 — Remove unnecessary snake_case fallbacks

Search the Passenger App codebase for defensive mappings supporting old/nonexistent formats.

Examples:
- isRead ?? is_read
- createdAt ?? created_at
- readAt ?? read_at
- userId ?? user_id
- tripId ?? trip_id
- stationId ?? station_id
- latitude ?? lat
- longitude ?? lng

Remove these fallback patterns where they are only used for backend compatibility.

The app should depend on the final backend camelCase contract only.

Example:

Before:
const isRead = notification.isRead ?? notification.is_read;

After:
const isRead = notification.isRead;

Keep the output model and UI behavior exactly the same.


TASK 2 — Normalize API response mappers

Review existing API mappers and adapters.

Focus on:
- Notifications
- Wallet
- Wallet transactions
- User profile
- Trips
- Shuttle bookings
- Ride details

Requirements:
- Every mapper must use the official backend response fields only.
- Remove duplicated field conversion logic.
- Keep one clear mapping path from API response to app model.
- Do not change existing app models unless required.
- Do not change any UI components.
- Preserve existing functionality.


TASK 3 — Add lightweight Zod validation for critical API responses

Add response validation only at important API boundaries.

Priority areas:
1. Authentication responses
2. User profile response
3. Notifications
4. Wallet
5. Wallet transactions
6. Shuttle trips/bookings
7. Ride details

Requirements:
- Use Zod schemas to validate incoming API data.
- Validation failures must be handled safely.
- Do not crash the application because of invalid data.
- Keep current error handling behavior.
- Do not rewrite the entire API layer.
- Do not add unnecessary complexity.

The purpose is:
- Detect backend contract changes early.
- Prevent silent UI bugs.
- Improve type safety.


TASK 4 — Review socket consumers

Review socket usage in the Passenger App.

Important:
Backend socket normalization is a separate task.

Do NOT modify backend assumptions.

For socket handlers:
- Keep handling defensive.
- Use optional fields where needed.
- Prevent crashes from missing fields.
- Do not add snake_case support.
- Do not rewrite socket architecture.

Notification socket behavior:
- REST notification API remains the source of truth for full notification objects.
- Socket notification events should only update realtime UI/toast behavior until backend normalization is completed.


TASK 5 — Do not touch these areas

Do NOT modify:
- UI components
- Styling
- Navigation
- Authentication flow
- Booking flow
- Payment flow
- Maps
- Socket architecture
- Context architecture
- Business logic


Verification required after completion:

Run:
1. TypeScript check:
tsc --noEmit

2. Android production export:
expo export --platform android

Confirm:
- No TypeScript errors.
- Build/export succeeds.
- No runtime-breaking changes.

Final response must include:

1. Complete list of changed files.
2. Exact reason for each file change.
3. Confirmation that backend files were not modified.
4. Verification results.
5. Remaining issues that require backend changes only.

Do not make any improvements outside this exact scope.