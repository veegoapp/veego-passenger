The attached ZIP is the complete Lovable UI implementation.

Treat it as the single design source of truth for the Passenger ride flow.

Your task is NOT to redesign, improve, modernize, reinterpret, simplify, or approximate anything.

Your task is to replace the current Passenger ride-flow UI with the Lovable UI as closely as possible.

Scope

Inspect the entire Lovable project first and identify every screen, bottom sheet, modal, card, dialog, overlay, and component related to the Passenger ride flow.

This includes (but is not limited to):

- Ride type / vehicle selection
- Vehicle option cards
- Payment method section
- Fare summary
- Searching for driver
- Driver assigned / accepted
- Driver information card
- Vehicle information
- Call driver
- Chat UI
- Cancellation flow
- Cancellation reasons
- Waiting states
- Driver arriving
- Ride in progress
- Trip completed
- Fare summary after trip
- Rating screen
- Receipt (if present)
- Any ride-related bottom sheet
- Any ride-related modal
- Any ride-related overlay
- Any ride-related animation
- Any ride-related transition

If another ride-related UI exists inside the Lovable project, include it as well.

Implementation Rules

Delete the existing Passenger ride-flow UI components where necessary.

Do not try to reuse old layouts simply because they already exist.

Rebuild the UI to match the Lovable implementation.

Keep the existing backend integration, APIs, socket events, navigation, business logic, state management, and data flow.

Only replace the presentation layer:

- Layout
- Components
- Typography
- Colors
- Icons
- Spacing
- Padding
- Margins
- Corner radius
- Shadows
- Borders
- Animations
- Transitions
- Visual hierarchy

Important

Do NOT:

- redesign
- improve
- modernize
- simplify
- optimize visually
- invent missing layouts
- mix old UI with Lovable UI
- create your own interpretation
- approximate the design

If something exists in the Lovable project, reproduce it.

The goal is visual parity with the Lovable implementation.

Assume the Lovable project is the correct design.

Before modifying files

First inspect the attached ZIP completely.

Create an internal inventory of every ride-flow UI element contained in the Lovable project.

Only after that begin replacing the existing Passenger ride-flow UI.

Do not stop halfway.

Replace the entire ride-flow UI in one pass.

After finishing

Run the available build/type checks.

Return only:

- Files changed
- Ride-flow screens/components replaced
- Anything from the Lovable ride flow that could not be reproduced exactly (and why)