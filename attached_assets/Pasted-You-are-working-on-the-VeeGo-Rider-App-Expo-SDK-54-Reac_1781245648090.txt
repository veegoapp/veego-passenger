You are working on the VeeGo Rider App (Expo SDK 54 / React Native / Expo Router). Backend base URL is EXPO_PUBLIC_API_URL. Auth token sent as Bearer. Complete all 4 tasks in order. After finishing, output a report titled "## What was done" listing every file created or modified per task.

---

TASK 1 — Safety / SOS screen during active car ride

Location: app/(tabs)/car.tsx and a new component components/shared/SafetySheet.tsx

What to build:
- During an active car ride (after driver assigned, before ride:completed), show a red shield button fixed at the bottom-right of the map screen
- Tapping it opens a bottom sheet modal: SafetySheet.tsx
- Inside SafetySheet, show 3 options:
  1. "Call 122" button → calls Linking.openURL('tel:122')
  2. "Share trip on WhatsApp" button → builds a WhatsApp message with: driver name, vehicle type, vehicle color, plate number, current GPS coordinates, and a Google Maps link using the current latitude/longitude → calls Linking.openURL('whatsapp://send?text=...')
  3. "Report emergency" button → calls POST /rides/:rideId/sos with { latitude, longitude, notes: 'SOS triggered by passenger' } → on success shows a confirmation message "Emergency services notified" and closes the sheet
- The SOS button must also emit the sos:triggered event data to the admin via the existing socket connection (the backend handles this on the server side via the POST endpoint — no extra socket emit needed from client)
- Use i18n for all strings (add keys to constants/i18n.ts for both EN and AR)
- Support RTL layout in the sheet

---

TASK 2 — Route deviation alert

Location: src/hooks/useRide.ts and app/(tabs)/car.tsx

What to build:
- Listen for the socket event ride:deviation_warning (payload: { rideId, driverLat, driverLng, deviationMeters, detectedAt })
- When received, show a non-blocking dismissable banner at the top of the active ride screen with the message: "Your driver may be off-route. Stay alert." (use i18n)
- The banner must have a Dismiss button
- The banner must auto-dismiss after 30 seconds if not manually dismissed
- Add the socket event constant RIDE_DEVIATION_WARNING to src/constants/socketEvents.ts if not already present
- Add i18n keys for both EN and AR

---

TASK 3 — Cancel ride with reason picker

Location: app/(tabs)/car.tsx

What to fix:
- Currently cancelling a ride opens a plain Alert with yes/no only
- Replace this with a bottom sheet that shows a list of cancel reasons:
  "Driver is too far away", "Wait time too long", "Wrong vehicle type", "Changed my mind", "Other"
- User must select one reason before the confirm button is enabled
- On confirm → call PATCH /rides/:id/cancel with { reason: selectedReason }
- Show a loading indicator on the confirm button while the request is in-flight
- On success → reset ride state
- On error → show an error message inside the sheet and keep it open
- Add i18n keys for all reason strings and button labels (EN + AR)

---

TASK 4 — Complete forgot password flow

Location: app/auth.tsx (or split into new screens app/otp.tsx and app/reset-password.tsx)

What to build — the full 3-step flow:
Step A: Phone/email submission (already exists — POST /auth/forgot-password)
Step B: OTP entry screen
  - 4 or 6 digit OTP input (individual boxes or single input)
  - "Verify" button → POST /auth/verify-otp with { phone, otp }
  - "Resend OTP" button (enabled after 60s countdown)
  - On success → navigate to Step C
Step C: New password form
  - "New password" + "Confirm password" fields
  - Submit → POST /auth/reset-password with { token (from OTP response), password }
  - On success → navigate to auth/login with a success message
- Add i18n keys for all new strings (EN + AR)
- Support RTL

---

After completing all 4 tasks output:
## What was done
- Task 1: [files created/modified, what changed]
- Task 2: [files created/modified, what changed]
- Task 3: [files modified, what changed]
- Task 4: [files created/modified, what changed]