You are working on the VeeGo Passenger App (React Native / Expo).

A full backend cross-check has been completed. The backend code is correct and does not need changes. The API contract documentation has already been updated to reflect the real backend behavior.

Your job is to apply exactly 7 targeted fixes to the frontend source files listed below. Do not refactor anything outside the scope of each fix. Do not change any file that is not listed.

---

## Context: what the cross-check revealed

- Most endpoint paths the frontend was already calling are correct.
- The backend contract documentation was wrong, not the frontend.
- Only the 7 issues below are confirmed frontend bugs that need code changes.

---

## Fix 1 — F-003: Rate driver wrong path
File: app/(tabs)/car.tsx

Change:
  api.post(`/rides/${rideId}/rate`, { rating, comment })

To:
  api.post(`/rides/${rideId}/rate-driver`, { rating, comment })

No other changes to this file beyond this line.

---

## Fix 2 — F-008 / B-005: Promo — wrong endpoint
File: src/hooks/usePromos.ts

The current code calls GET /promo (admin-only) to list all promos.
This must be replaced with a validate flow: the user types a code, and the app calls POST /promo/validate.

Remove: any call to GET /promo or GET /promos
Add: a validatePromo(code: string) function that calls:
  api.post('/promo/validate', { code })
  and returns { valid: boolean, discount: number, type: string }

Export validatePromo so the promo UI screen can call it when the user submits a code.
Do not auto-fetch promos on mount anymore.

---

## Fix 3 — B-007: Wallet top-up not wired
File: src/hooks/useWallet.ts  (and wallet.tsx if the button handler lives there)

Add a topUp(amount: number, paymentMethod: string) function that calls:
  api.post('/wallet/topup', { amount, paymentMethod })

Wire it to the existing top-up button in wallet.tsx.
After a successful top-up, re-fetch the wallet balance (call the existing balance fetch).

---

## Fix 4 — S-004: Waiting charge events not handled
File: src/hooks/useRide.ts

The backend emits 3 live socket events when a driver is waiting. Add listeners for all three:

  socket.on('ride:waiting:charge:started', () => {
    // show UI warning: waiting charges have begun
  })

  socket.on('ride:waiting:charge:updated', ({ charge }) => {
    // update displayed waiting charge amount
  })

  socket.on('ride:waiting:charge:capped', ({ charge }) => {
    // show UI notice: waiting charge has reached its cap
  })

Expose the waiting charge state (amount + status) from the hook so car.tsx can display it.
Clean up all three listeners on unmount.

---

## Fix 5 — S-005: Surge pricing wrong event name
File: src/hooks/useRide.ts  (or car.tsx — wherever surge:update is currently listened to)

Change:
  socket.on('surge:update', handler)

To:
  socket.on('surge:updated', handler)

One character difference. No logic change needed.

---

## Fix 6 — B-001: SOS button missing
File: app/(tabs)/car.tsx

This is a live safety feature on the backend (POST /rides/:id/sos). It has no UI.

Add an SOS button that is visible only when a ride is in an active state (status: 'accepted' | 'arrived' | 'in_progress').

On press:
  api.post(`/rides/${activeRideId}/sos`)

The button should be visually distinct (red, clearly labeled "SOS"). Keep it simple — no confirmation dialog needed, immediate call on press.

---

## Fix 7 — Mock data cleanup
File: src/constants/data.ts

Remove the following exports that are no longer used now that real API hooks exist:
  - upcomingTrips
  - pastTrips
  - notifications (the static array — keep the type/interface if defined here)

Before removing each export, confirm it is not imported anywhere else in the codebase. If any component still imports from these exports, update that component to use the correct hook instead:
  - upcomingTrips / pastTrips → useTrips()
  - notifications → useNotifications()

Do not remove routes or stations from this file — those are still under review.

---

## What NOT to change

- Do not touch any auth files (login, register, forgot-password — these were confirmed correct).
- Do not change any Socket.IO event names other than surge:update → surge:updated.
- Do not change any REST endpoint paths other than the rate-driver fix in Fix 1.
- Do not add new dependencies.
- Do not change the BASE_URL or Axios client configuration.

---

## When done

Provide a summary listing:
1. Each file modified
2. The exact line(s) changed
3. Any component that was updated as a side effect of Fix 7 (mock data cleanup)

Do not generate an audit report. Just make the changes and summarise what was done.