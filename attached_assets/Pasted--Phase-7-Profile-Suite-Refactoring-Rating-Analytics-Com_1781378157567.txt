
Phase 7 : Profile Suite Refactoring & Rating Analytics]
​Component Target: ProfileScreen, PersonalInformationScreen, RatingDetailsScreen (New), and Navigator files.
​Tasks to execute:
​Delete Security Screen & Update Navigation:
​Completely remove/delete the SecurityScreen component (134616.jpg) from the codebase.
​Remove the "Security" row item from the main ProfileScreen list and update the navigation layout smoothly so it doesn't cause any routing crashes.
​Personal Information Field Restrictions & Profile Picture Integration:
​Read-Only Fields: In PersonalInformationScreen (134615.jpg), change the FULL NAME and PHONE NUMBER inputs to be permanently disabled/read-only. Style them with a slightly muted tint to visually signal to the user that they cannot be modified.
​Editable Fields: Ensure EMAIL ADDRESS and DATE OF BIRTH remain fully editable text inputs.
​Add Password Change field: Add a secured text input field or a dedicated button to "Change Password" within this screen.
​Avatar Upload Feature: At the top of the personal info form, implement an interactive Avatar container (Profile Circle) displaying the user's current picture. Add an "Edit/Camera" icon badge. On press, trigger an image picker (e.g., react-native-image-picker).
​Avatar Code Placeholder: Since there's no reference backend endpoint for updating the image yet, write a clean local state upload function and flag the missing endpoint with a clear comment: // TODO: Provide User Avatar Upload API Endpoint.
​Live Profile Header Stats & Dynamic Checking:
​Inside the dark top card on the main ProfileScreen (134614.jpg), remove mock data for Trips (24), Rating (4.9), and Saved (48 EGP).
​Bind these fields to read directly from the authenticated user profile state/backend response. If these metrics aren't part of the main user object, add a // TODO: Request Profile Stats API flag.
​Create a New Read-Only Rating Analytics Screen:
​When the user taps on the Rating section in the top profile card, navigate them to a newly created RatingDetailsScreen.
​Layout Design: This screen must be a clean, read-only dashboard layout showing the overall score (e.g., 4.9) and a detailed vertical breakdown bar chart (Rating Distribution):
​5 Stars: Progress Bar %
​4 Stars: Progress Bar %
​3 Stars: Progress Bar %
​2 Stars: Progress Bar %
​1 Star: Progress Bar %
​Use modern, sleek minimalist progress bars that perfectly match the app’s general theme.
​Universal API Audit across the Profile Suite:
​Audit all fields, switches (Dark Mode, Push Notifications, Language, Privacy toggles), and data rows across all profile sub-pages (134617.jpg, 134618.jpg).
​For any toggle or data field missing an active frontend integration or endpoint path, append a noticeable comment block requesting the API details from the user.
