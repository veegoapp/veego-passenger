Continue and complete the Bottom Tab Bar overlap fix in the VeeGo Passenger App.

Important: Do NOT make any changes outside this specific issue. Do NOT refactor unrelated code. Do NOT hide the Bottom Tab Bar. The Bottom Tab Bar must remain visible exactly as it is.

Problem:
The Bottom Tab Bar is overlapping content in screens and bottom sheets. Some UI elements that appear from the bottom (searching for driver card, vehicle selection sheet, ride options, driver assigned card, etc.) are partially hidden behind the Bottom Tab Bar.

Expected behavior:
- The Bottom Tab Bar stays visible.
- All normal pages and all bottom sheets/cards must start above the Bottom Tab Bar area.
- Buttons, inputs, and actions at the bottom of any sheet must remain fully visible and clickable.
- The fix should work across different screen sizes and safe areas.

Previous investigation already identified the root cause:

Root Cause:
The tab bar is positioned as:
- position: absolute
- bottom: 0
- zIndex: 99

Its real height is larger than only the device safe area.

The affected bottom sheets currently only use:
- insets.bottom + 16
- insets.bottom + 35

This clears only the phone safe area but does not reserve space for the Bottom Tab Bar, causing content to be hidden behind it.

Files involved:
- app/(tabs)/_layout.tsx
- context/TabBarContext.tsx
- app/(tabs)/index.tsx
- components/car/DriverSearching.tsx
- components/car/DriverAssignedCard.tsx
- components/car/RideOptionsSheet.tsx
- components/car/CarServiceScreen.tsx

Implementation requirement:
Use a clean shared solution.

Preferred approach:
1. Measure the actual Bottom Tab Bar height using onLayout in the tab bar component.
2. Store that height in TabBarContext.
3. Expose tabBarHeight through the context.
4. Update affected bottom sheets/cards to use the measured tabBarHeight instead of hardcoded padding values.
5. Keep safe area handling correct.
6. Avoid adding random fixed pixel values.

Before editing:
- Inspect the current code state because previous changes may be partially applied.
- Identify what has already changed.
- Continue only the missing parts.

After editing:
- Run TypeScript checks.
- Report exactly which files changed and why.
- Do not modify any unrelated functionality.