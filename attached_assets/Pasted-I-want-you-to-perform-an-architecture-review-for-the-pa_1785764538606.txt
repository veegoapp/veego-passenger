I want you to perform an architecture review for the passenger app map system.

Current situation:
The app currently has two different map components:
1. CarMap.tsx
2. PassengerTrackingMap.native.tsx

They have overlapping responsibilities:
- Driver marker rendering
- Driver location tracking
- Route/polyline drawing
- Directions API calls
- Camera control
- ETA handling

We want to clean this architecture and rebuild the map layer properly instead of keeping duplicated logic.

Do NOT modify code yet.
First provide an implementation plan.

Analyze the current architecture and answer:

1. Current state analysis
- What responsibilities belong to CarMap?
- What responsibilities belong to PassengerTrackingMap?
- What logic is duplicated between them?
- What logic should remain separate?

2. Recommended future architecture
Design the clean architecture you would build.

Explain:
- Should we have one shared Map component?
- What should be extracted into reusable components/hooks?
- What should stay inside service-specific screens?
- How should Car/Scooter/Delivery and Shuttle share the map system without becoming complicated?

3. Migration strategy
Provide a step-by-step migration plan:

Phase 1:
What should be extracted first?

Phase 2:
How do we move existing flows safely?

Phase 3:
How do we remove the old duplicated code?

For each phase explain:
- Files affected
- Expected changes
- Why this order is safer

4. Risk assessment
List the risks of this refactor:

Technical risks:
- Breaking active rides
- Breaking navigation
- Breaking socket-driven location updates
- Breaking shuttle tracking
- Breaking directions/polyline rendering
- Breaking camera behavior

For each risk:
- Probability (Low/Medium/High)
- Impact (Low/Medium/High)
- How to reduce the risk

5. Testing strategy
Before removing old code:
What scenarios must be tested?

Include:
- Car booking
- Scooter booking
- Delivery booking
- Driver accepted ride
- Driver arriving
- Trip started
- Trip completed
- Shuttle tracking
- App restart during active ride
- Deep link tracking

6. Final recommendation
Give a clear answer:

Is it better to:
A) Keep both maps and only extract shared logic?
B) Replace both with a new unified map architecture?
C) Keep CarMap and convert PassengerTrackingMap into a wrapper?
D) Another approach?

Choose one and explain why.

Important:
- Do not focus on the previous route bug.
- This is an architecture and maintainability review only.
- Base your answer on the actual code structure.
- Do not suggest a full rewrite unless justified.
- Optimize for production stability, not just fewer files.