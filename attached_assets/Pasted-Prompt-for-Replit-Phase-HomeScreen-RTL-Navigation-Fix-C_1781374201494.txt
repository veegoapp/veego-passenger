Prompt for Replit - Phase: HomeScreen & RTL Navigation Fix]
​Component Target: HomeScreen & Bottom Tab Navigator Configuration (e.g., AppNavigator.js or BottomTab.js)
​Tasks to execute:
​Dynamic Greeting & User Avatar (Top Bar):
​Greeting Logic: Replace the static "GOOD MORNING VeeGo" text. Implement a dynamic greeting based on the user's local time:
​From 5:00 AM to 11:59 AM: "Good morning, [First Name]"
​From 12:00 PM to 4:59 PM: "Good afternoon, [First Name]"
​From 5:00 PM to 4:59 AM: "Good evening, [First Name]"
​Avatar Initials: The top-right profile circle (VG) must dynamically display the first letter of the user's First Name and Last Name (e.g., John Doe -> JD).
​Navigation: Wrap the Avatar circle in a TouchableOpacity (or Pressable) and configure it to navigate to the ProfileScreen when pressed.
​Services List Layout (Horizontal Grid Refactoring):
​Change the container style of the service icons from a square block to a compact horizontal rectangle (reduce the current height by roughly 50%).
​Content Layout: Inside each service card, align the Icon/Logo horizontally next to the Service Name (e.g., Icon + "Shuttle" in a single row). Ensure layout flexibility for both LTR and RTL orientations.
​"Soon" Badge styling: For unavailable services ("Car", "Scooter", "Delivery"), extract the "So on / Soon" text from the main flow. Display it as a small, elegant badge floating on the top corner of the icon/card with a soft, semi-transparent background color and minimal font size, ensuring it doesn't clutter the reduced rectangle height.
​Bottom Navigation Bar (RTL / Language-Switching Fix):
​Bug: The active tab text and icon disappear/glitch specifically after implementing localization (Arabic/English toggle) due to RTL/LTR layout shifting.
​Fix:
​Review the Tab Bar styles. Replace any fixed directional styling (like marginLeft, paddingRight, or absolute positioning left/right inside the tab button) with localized-safe properties or use I18nManager.isRTL to conditionally handle positions.
​Ensure the active tab container uses proper flex layouts (flexDirection: 'row' or 'column') that adapt naturally to direction changes without causing layout clipping, text truncation, or hiding elements under the screen boundaries.
​Featured Offers (Promo Cards Accordance):
​Data Fetching: Ensure the promo data ("Featured offers") is dynamically fetched from the Backend API instead of being hardcoded.
​Layout Consistency: Fix the height misalignment between the promo cards (e.g., "New Rider" card vs "Weekly Pass" card). Apply consistent padding, height constraints, and structural alignment so all promo cards render with identical dimensions regardless of text length.